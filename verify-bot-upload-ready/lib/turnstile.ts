export async function validateTurnstile(token:string,ip:string){const s=process.env.TURNSTILE_SECRET_KEY;if(!s)throw new Error("TURNSTILE_SECRET_KEY missing");const f=new URLSearchParams({secret:s,response:token});if(ip!=="unknown")f.set("remoteip",ip);const r=await fetch("https://challenges.cloudflare.com/turnstile/v0/siteverify",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:f,cache:"no-store"});if(!r.ok)return{success:false};return r.json() as Promise<{success:boolean}>}
