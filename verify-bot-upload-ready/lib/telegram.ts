import {escapeHtml} from "./security";export async function sendTelegramLog(fields:Record<string,string>){const t=process.env.TELEGRAM_BOT_TOKEN,c=process.env.TELEGRAM_CHAT_ID;if(!t||!c)throw new Error("Telegram config missing");const text=Object.entries(fields).map(([k,v])=>`<b>${escapeHtml(k)}</b>: ${escapeHtml(v)}`).join("\n");const r=await fetch(`https://api.telegram.org/bot${t}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:c,text:`🔐 <b>Discord Verification Log</b>\n\n${text}`,parse_mode:"HTML"}),cache:"no-store"});if(!r.ok)throw new Error(`Telegram ${r.status}`)}
